const Discord = require('discord.js');

/*case "hentai":
                            if(!message.channel.nsfw)return message.reply('Va dans un salon nsfw!')
                            var hentai = new Discord.RichEmbed()
                            superagent.get('https://nekobot.xyz/api/image')
                            .query({ type: 'hentai_anal'})
                            .end((err, response) => {
                                 message.channel.send({embed:
                                    hentai.setFooter('Liberty [BOT]')
                                    .setImage(response.body.message)
                                    .setColor("RANDOM")});
                                 })
                                 break;

                        case "pussy":
                            if(!message.channel.nsfw)return message.reply('Va dans un salon nsfw!')
                            var pussy = new Discord.RichEmbed()
                            superagent.get('https://nekobot.xyz/api/image')
                            .query({ type: 'pussy'})
                            .end((err, response) => {
                                 message.channel.send({embed:
                                    pussy.setFooter('Liberty [BOT]')
                                    .setImage(response.body.message)
                                    .setColor("RANDOM")});
                                 })
                                 break;

                        case "ass":
                            if(!message.channel.nsfw)return message.reply('Va dans un salon nsfw!')
                            var ass = new Discord.RichEmbed()
                            superagent.get('https://nekobot.xyz/api/image')
                            .query({ type: 'ass'})
                            .end((err, response) => {
                                 message.channel.send({embed:
                                    ass.setFooter(`${bot.user.username}`)
                                    .setImage(response.body.message)
                                    .setColor("RANDOM")});
                                 })
                                 break;

                        case "anal":
                            if(!message.channel.nsfw)return message.reply('Va dans un salon nsfw!')
                            var anal = new Discord.RichEmbed()
                            superagent.get('https://nekobot.xyz/api/image')
                            .query({ type: 'anal'})
                            .end((err, response) => {
                                 message.channel.send({embed:
                                    anal.setFooter(`${bot.user.username}`)
                                    .setImage(response.body.message)
                                    .setColor("RANDOM")});
                                 })
                                 break;
                                 
                                 */

//a toi de mettre le cmd hundler etc...